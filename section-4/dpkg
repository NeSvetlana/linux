Проблема с зависимостями пакетов, dpkg не работает с зависимостями

dpkg: зависимости пакетов не позволяют настроить пакет tor:
 tor зависит от libevent-2.1-6 (>= 2.1.8-stable), однако:
  Пакет libevent-2.1-6 не установлен.
 tor зависит от libssl1.1 (>= 1.1.0), однако:
  Пакет libssl1.1 не установлен.

dpkg: ошибка при обработке пакета tor (--install):
 проблемы зависимостей — оставляем не настроенным
Обрабатываются триггеры для man-db (2.10.2-1) …
При обработке следующих пакетов произошли ошибки:
 tor

Так как при установке возникло предупреждение о том, что есть более новая версия, чем наша, установим её
 
dpkg: предупреждение: снижение версии tor с 0.4.6.10-1 до 0.3.2.10-1

Решение:

$ sudo apt install tor

Проверим версию:

$ apt show tor

Package: tor
Version: 0.4.6.10-1
Priority: optional


 
